rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write for this application as it uses a custom authentication logic
    // and does not currently use Firebase Authentication (request.auth).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
